// initialize array of name
var array = ['Hairs', 'Subhan', 'Khidu', 'Tani'];
// loop through each item and display msg
array.forEach(function (friend) {
    console.log("Hello ".concat(friend));
});
